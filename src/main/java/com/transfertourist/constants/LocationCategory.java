package com.transfertourist.constants;

/**
 * Category a {@code location} belongs to. Mirrors the frontend
 * {@code LocationCategory} union so the Locations page grouping stays in sync.
 */
public enum LocationCategory {
    AIRPORT,
    CITY,
    HOTEL,
    RESORT
}
